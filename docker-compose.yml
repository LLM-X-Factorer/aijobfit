services:
  aijobfit:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: https://aijobfit.llmxfactor.cloud
    container_name: aijobfit
    restart: unless-stopped
    ports:
      - "3004:3000"
